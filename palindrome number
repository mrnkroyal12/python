n=int(input("enter the number"))
rem=0
sum=0
temp=n
while(n != 0):
    rem=n%10
    sum=sum*10+rem
    n=n//10
if(sum == temp):
    print("palindrome")
else:
    print("not a palindrome")

    output: 123
    not a palindrome
